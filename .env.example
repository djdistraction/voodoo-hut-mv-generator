# ─── LLM (Groq — free tier) ──────────────────────────────────────────────────
# Sign up free at console.groq.com
GROQ_API_KEY=gsk_...
GROQ_MODEL=llama-3.3-70b-versatile

# ─── Image Generation (Hugging Face Inference Providers) ─────────────────────
# Sign up free at huggingface.co → Settings → Access Tokens → New token (read).
# NOTE: HF retired the old free serverless API (api-inference.huggingface.co).
# Image generation now runs through Inference Providers and is billed against
# your HF account (free accounts get a small monthly credit).
HF_TOKEN=hf_...
HF_IMAGE_MODEL=black-forest-labs/FLUX.1-schnell
# Provider routing: "auto" lets HF pick an available provider. Pin a specific
# one if needed: fal-ai | replicate | nebius | together | hf-inference | ...
HF_PROVIDER=auto

# ─── Audio Transcription (local Whisper — no key needed) ─────────────────────
# Options: tiny (fastest) | base | small | medium (most accurate on CPU)
WHISPER_MODEL=base

# ─── Storage (local filesystem — no account needed) ──────────────────────────
STORAGE_BACKEND=local
LOCAL_STORAGE_PATH=./storage

# ─── Database (SQLite — no server needed) ────────────────────────────────────
DATABASE_URL=sqlite+aiosqlite:///./voodoo.db

# ─── Task Queue (in-memory — no Redis needed) ────────────────────────────────
CELERY_BROKER_URL=memory://
CELERY_RESULT_BACKEND=cache+memory://

# ─── Video Generation ────────────────────────────────────────────────────────
# "ffmpeg" = free Ken Burns effect (no GPU needed)
# "runway" = RunwayML Gen-4 (~$0.05/clip, requires key below)
VIDEO_BACKEND=ffmpeg
VIDEO_FPS=25
CLIP_DURATION=5
OUTPUT_RESOLUTION=1920x1080

# Only needed if VIDEO_BACKEND=runway
RUNWAY_API_KEY=

# ─── Future: paid/cloud upgrades ─────────────────────────────────────────────
# Uncomment and fill these in when you're ready to deploy or upgrade quality

# Cloudflare R2 (set STORAGE_BACKEND=r2)
# R2_ACCOUNT_ID=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET_NAME=voodoo-mv

# Supabase (set DATABASE_URL=postgresql+asyncpg://...)
# DATABASE_URL=postgresql+asyncpg://postgres:[password]@db.[ref].supabase.co:5432/postgres

# Upstash Redis (set CELERY_BROKER_URL=rediss://...)
# CELERY_BROKER_URL=rediss://:[password]@[host]:6379
